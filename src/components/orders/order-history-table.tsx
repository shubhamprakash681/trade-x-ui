import Link from "next/link";
import type { OrderResponse } from "@/types/api.types";
import { formatCurrency, formatDateTime, formatQuantity } from "@/lib/utils";

export function OrderHistoryTable({ orders }: { orders: OrderResponse[] }) {
  if (!orders.length) return <p className="py-12 text-center text-sm text-text-secondary">No orders have been placed yet.</p>;
  return <div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-border-primary text-xs uppercase tracking-wide text-text-tertiary"><tr><th className="px-5 py-3 font-medium">Order</th><th className="px-5 py-3 font-medium">Instrument</th><th className="px-5 py-3 font-medium">Side</th><th className="px-5 py-3 font-medium">Quantity</th><th className="px-5 py-3 font-medium">Execution price</th><th className="px-5 py-3 font-medium">Amount</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 font-medium">Created</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id} className="border-b border-border-primary last:border-0 hover:bg-bg-tertiary"><td className="px-5 py-4 text-text-secondary">#{order.id}</td><td className="px-5 py-4"><Link href={`/stocks/${encodeURIComponent(order.symbol)}`} className="font-semibold text-text-primary hover:text-brand">{order.symbol}</Link><p className="mt-0.5 max-w-40 truncate text-xs text-text-secondary">{order.stockName}</p></td><td className="px-5 py-4"><span className={`rounded px-2 py-1 text-xs font-semibold ${order.side === "BUY" ? "bg-profit-bg text-profit" : "bg-loss-bg text-loss"}`}>{order.side}</span></td><td className="px-5 py-4 text-text-primary">{formatQuantity(order.quantity)}</td><td className="px-5 py-4 text-text-primary">{formatCurrency(order.price)}</td><td className="px-5 py-4 text-text-primary">{formatCurrency(order.totalAmount)}</td><td className="px-5 py-4"><span className="rounded bg-profit-bg px-2 py-1 text-xs font-semibold text-profit">{order.status}</span></td><td className="px-5 py-4 text-text-secondary">{formatDateTime(order.createdAt)}</td></tr>)}</tbody></table></div>;
}
